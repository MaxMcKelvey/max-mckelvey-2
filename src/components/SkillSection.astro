---
export interface Props {
	title: string;
}

const { href, title, body } = Astro.props;
---

<div class="md:w-1/3 flex flex-col">
    <h4 class="text-lg self-center">{title}</h4>
    <div class="self-center w-3/4 text-sm">
        <slot />
    </div>
</div>